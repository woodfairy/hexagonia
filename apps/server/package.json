{
  "name": "@hexagonia/server",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npm run build -w @hexagonia/shared && npm run build -w @hexagonia/rules && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "echo \"Server integration tests pending\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "format": "echo \"Formatting handled at repo level\""
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.0.0",
    "@fastify/websocket": "^11.0.2",
    "@hexagonia/rules": "file:../../packages/rules",
    "@hexagonia/shared": "file:../../packages/shared",
    "argon2": "^0.44.0",
    "fastify": "^5.2.1",
    "pg": "^8.14.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11",
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
