{
  "name": "@hexagonia/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json --pretty false",
    "test": "pnpm run test:unit && pnpm run test:regression",
    "test:unit": "echo \"No unit tests in shared\"",
    "test:regression": "echo \"No regression tests in shared\"",
    "lint": "pnpm run typecheck",
    "format": "echo \"Formatting handled at repo level\""
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
